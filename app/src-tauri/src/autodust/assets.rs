//! Download + integrity-check the two ONNX models for AI dust removal.
//! Assets live under <app_data>/autodust/. The ONNX Runtime native library is
//! shared with the upscaler (see `crate::upscale::assets`) — this module only
//! ships the two model files.
// Real download/verify logic is filled in Task 2.2.
